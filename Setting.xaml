<Window x:Class="Statting_Simulator.Setting"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Statting_Simulator"
        mc:Ignorable="d"
        Title="Setting" Height="195" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="15" Text="대장장이 숙련도"/>
            <TextBlock Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="15" Text="TEC 스탯"/>
            <TextBox x:Name="PBox" FontSize="15" Margin="10" Grid.Column="1"/>
            <TextBox x:Name="TBox" FontSize="15" Margin="10" Grid.Column="1" Grid.Row="1" IsEnabled="False"/>
        </Grid>
        <Button Grid.Row="1" Margin="50,2" Content="확인" Click="Close"/>
    </Grid>
</Window>
